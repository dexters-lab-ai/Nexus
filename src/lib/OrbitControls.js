import {
  EventDispatcher,
  MOUSE,
  Quaternion,
  Spherical,
  TOUCH,
  Vector2,
  Vector3
} from 'three';

export class OrbitControls extends EventDispatcher {
  // Implementation copied from three.js examples
  // but with all imports converted to CDN URLs
  constructor(object, domElement) {
    super();
    // ... rest of OrbitControls implementation
  }
}
