# Force npm to use the package-lock.json
package-lock=true

# Ignore optional dependencies to prevent installation issues
optional=false

# Set the engine-strict flag to false to prevent engine check failures
engine-strict=false

# Set the platform and architecture
platform=linux
arch=x64

# Increase network timeout to prevent timeouts during installation
timeout=60000
